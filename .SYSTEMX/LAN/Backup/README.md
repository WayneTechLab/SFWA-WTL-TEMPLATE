# SYSTEMX LAN Backup

Ignored local pre-write snapshots belong here. The builder must create a backup
before any future source-editing action.

Each backup directory is timestamped and should preserve the path being changed.
Use it to compare, restore, or audit a local builder write. Backups are local
operator evidence only and must not contain secrets or live customer data.
