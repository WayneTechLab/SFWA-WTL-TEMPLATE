# PROMPT INJECT — Implement the SYSTEMX LAN Webflow-Class Master Plan

You are working inside `WayneTechLab/SFWA-WTL-TEMPLATE`.

## Read first

1. Current repository root `README.md`, `package.json`, `vite.config.ts`.
2. All current `.SYSTEMX/LAN/` plans, server, UI, contracts, importers, runtime/session files.
3. `.SYSTEMX/scripts/assert-lan-isolation.mjs` and current build/security/deploy scripts.
4. Current `wiki/` pages and sidebar.
5. This package's `.SYSTEMX/LAN/Research/Webflow/` documents, `planning/` CSVs, and proposed contracts.

## Global goal

Implement a repository-native visual Designer with a canonical document and command kernel. Preserve the existing LAN's loopback, token, allowlist, backup, evidence, and public-isolation controls.

## Execution order

Start with **Wave 0 only** unless the repository already contains verified completion evidence for it.

1. Inspect the latest Git state and current differences.
2. Create/update `.SYSTEMX` mission, wave, TODO, DONE, decision, recovery, and status records using the repository's current conventions.
3. Repair P0 wiki/security/command/path/version drift.
4. Add characterization tests for existing LAN behavior and safety boundaries.
5. Add a generated/validated capability and command manifest.
6. Run the documented verification suite and repair failures.
7. Produce a bounded commit/diff and update status/evidence.
8. Do not begin the next wave until Wave 0 exit criteria pass.

## Hard rules

- Do not expose `.SYSTEMX/LAN` through public routes or build output.
- Do not weaken host/origin/session/path/confirmation/backup/secret/evidence controls.
- Do not add a generic shell, filesystem proxy, SQL console, or cloud-action proxy.
- Do not paste or request live secrets in chat/context.
- Do not claim a feature is supported without automated evidence.
- Do not use exact-string replacement for new structural editing features.
- Do not rewrite unsupported source; label it read-only or needs-review.
- Do not enable live provider writes before emulator/test preflight and exact target approval.
- Do not copy Webflow code, assets, wording, or trade dress.
- Keep changes small, reversible, and synchronized across code, tests, `.SYSTEMX`, README, and wiki.

## Required final output for each wave

- files changed;
- commands/tests run and exact results;
- security/target checks;
- semantic and source diff summary;
- migration/rollback notes;
- unresolved blockers/risks;
- updated TODO/DONE/status/evidence;
- next wave entry criteria.
