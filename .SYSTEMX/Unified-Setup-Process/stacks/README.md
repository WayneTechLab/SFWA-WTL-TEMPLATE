# Stack Modes

Stack mode is a first-class WSG setup choice that is resolved before edition.

- `google-firebase`: default WSG stack.
- `microsoft-365`: Microsoft 365 sender/tenant path while preserving Firebase/GCloud unless explicitly changed.
- `custom`: explicit service selection only; never default to Supabase.
