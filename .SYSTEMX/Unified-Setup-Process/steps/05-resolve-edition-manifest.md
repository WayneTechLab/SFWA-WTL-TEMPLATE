# Step 05 — Resolve Edition Manifest

## Goal

Selected edition determines modules, page cap, required tools, optional tools,
and setup outputs.

## Actions

- Read the selected JSON file from `editions/`.
- Confirm enabled modules and page cap.
- Map modules to docs in `modules/`.

## Gate

The operator can list enabled modules and the page limit.
