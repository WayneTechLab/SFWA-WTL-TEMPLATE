# Step 08 — Scaffold Or Update App Structure

## Goal

The starter app and enabled module placeholders are aligned.

## Actions

- Use `.SYSTEMX/Template/starter` for fresh apps.
- Preserve existing app code for forks.
- Add only placeholders required by selected modules.

## Gate

`npm run typecheck`, `npm run lint`, and `npm run build` are expected to pass
after implementation.
