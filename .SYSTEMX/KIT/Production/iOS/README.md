# iOS Brand Package

Drag `Assets.xcassets` into Xcode. Set the app icon source to `AppIcon`. `BrandLogo` supplies 1x/2x/3x transparent images. App icons use a white background because Apple app icons must not contain transparency.
