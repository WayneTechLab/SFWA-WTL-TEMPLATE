# SFWA-WTL-G1 — Template working files
# Generated by setup.sh / WSG-MENU.sh — may contain project IDs, domains,
# Firebase per-platform config, and a Sentry DSN. Keep OUT of version control.
interview.answers

# Downloaded Firebase platform config files (never commit)
GoogleService-Info.plist
google-services.json
*.local
